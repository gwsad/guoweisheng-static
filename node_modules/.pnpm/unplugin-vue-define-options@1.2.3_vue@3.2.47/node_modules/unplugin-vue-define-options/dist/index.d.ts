import * as unplugin from 'unplugin';
import { MarkRequired } from '@vue-macros/common';
import { FilterPattern } from '@rollup/pluginutils';

declare function transformDefineOptions(code: string, id: string): {
    code: string;
    map: any;
} | undefined;

interface Options {
    include?: FilterPattern;
    exclude?: FilterPattern;
    version?: 2 | 3;
}
type OptionsResolved = MarkRequired<Options, 'include' | 'version'>;
declare const _default: unplugin.UnpluginInstance<Options | undefined, false>;

export { Options, OptionsResolved, _default as default, transformDefineOptions as transform };
